# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Don't ignore data directory
!data/*.json
!public/eipa.db.json

# BNetzA raw CSV files (large, fetched by CI)
data/bnetza/*.csv

# IRVE raw CSV files (large, fetched by CI)
data/irve/*.csv

# NDW raw JSON files (large, fetched by CI)
data/ndw/*.json

# BEEV raw JSON files (large, fetched by CI)
data/beev/*.json
